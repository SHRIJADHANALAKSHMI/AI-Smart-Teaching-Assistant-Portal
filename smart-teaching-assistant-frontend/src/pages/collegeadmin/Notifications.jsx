import { Bell, Loader2 } from "lucide-react";
import { useCollegeAdmin } from "../../context/CollegeAdminContext";

export default function Notifications() {
    const { notifications, loading, error } = useCollegeAdmin();
    return <section className="space-y-6"><header className="rounded-3xl border border-slate-200 bg-white p-7 shadow-sm"><h1 className="flex items-center gap-3 text-3xl font-extrabold text-slate-900"><Bell className="text-emerald-600" />Invitation activity</h1><p className="mt-2 text-slate-500">This activity is generated from the live professor invitation records.</p></header>{error && <div className="rounded-xl bg-red-50 p-4 text-red-700">{error}</div>}<div className="rounded-3xl border border-slate-200 bg-white shadow-sm">{loading ? <div className="p-12 text-center"><Loader2 className="mx-auto animate-spin text-emerald-600" /></div> : notifications.length ? <ul>{notifications.map((notification) => <li key={notification.id} className="border-b border-slate-100 p-5 last:border-0"><div className="flex items-start gap-4"><div className="mt-1 h-3 w-3 rounded-full bg-emerald-500" /><div><p className="font-bold text-slate-900">{notification.title}</p><p className="mt-1 text-slate-600">{notification.message}</p><p className="mt-2 text-sm text-slate-400">{notification.time}</p></div></div></li>)}</ul> : <p className="p-12 text-center text-slate-500">No invitation activity yet.</p>}</div></section>;
}
